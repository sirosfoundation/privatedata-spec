{
  "id": "metadata-preservation-001",
  "description": "Credential and keypair metadata must be preserved exactly",
  "version": "3",
  "fields": {
    "credentials": [
      "format",
      "kid",
      "instanceId",
      "batchId",
      "credentialIssuerIdentifier",
      "credentialConfigurationId"
    ],
    "keypairs": ["did", "algorithm", "publicKey", "createdAt"],
    "settings": ["openidRefreshTokenMaxAgeInSeconds"]
  },
  "expected": "all fields retained through decrypt/re-encrypt"
}
